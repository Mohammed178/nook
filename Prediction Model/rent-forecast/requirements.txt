# Pinned to the versions actually installed in .venv (pip freeze).
# Note: resolved versions are newer than the plan's section 2 table
# (e.g. pandas 3.0, numpy 2.4, sklearn 1.9) — pinned to what runs here.

# Core data / numerics
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1

# Modeling
scikit-learn==1.9.0
statsmodels==0.14.6
patsy==1.0.2

# Visualisation
matplotlib==3.11.0
seaborn==0.13.2

# Persistence
joblib==1.5.3

# Serving layer (phase 6)
fastapi==0.139.0
starlette==1.3.1
uvicorn==0.50.0
pydantic==2.13.4
python-multipart==0.0.32  # required by FastAPI for CSV file uploads
httpx==0.28.1

# Notebooks / testing
nbformat==5.10.4
pytest==9.1.1
